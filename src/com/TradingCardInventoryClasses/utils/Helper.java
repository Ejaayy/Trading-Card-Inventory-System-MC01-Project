package com.TradingCardInventoryClasses.utils;

public class Helper {
    /*
    TO DO: BE OPTIMIZED FOR USER INPUT

    private Card promptCardFromCollection() {
        Card foundCard;
        do {
            System.out.print("Enter Card Name from Collection: ");
            String cardName = scanner.nextLine();
            foundCard = collection.searchCard(cardName);
            if (foundCard == null) {
                System.out.println("Card not found.");
            }
        } while (foundCard == null);
        return foundCard;
    }

    private Binder promptBinder() {
        Binder foundBinder;
        do {
            System.out.print("Enter Binder Name: ");
            String binderName = scanner.nextLine();
            foundBinder = manageBinder.searchBinder(binderName);
            if (foundBinder == null) {
                System.out.println("Binder not found.");
            }
        } while (foundBinder == null);
        return foundBinder;
    }

     */
}
